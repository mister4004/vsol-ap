// build.gradle.kts (корневой)

plugins {
    // Другие плагины
    id("com.android.application") version "8.1.1" apply false
    id("org.jetbrains.kotlin.android") version "1.9.25" apply false
    id("com.google.gms.google-services") version "4.4.2" apply false // Добавляем плагин Google Services
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
